from setuptools import setup, find_packages

setup(
    name="ashendb",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        "aiofiles",
        # Add any other dependencies here.
    ],
)
